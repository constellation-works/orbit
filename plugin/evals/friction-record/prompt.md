---
description: A new tooling problem becomes its own friction record with attribution, not a task and not an edit to an unrelated record.
tags: [friction]
max_turns: 15
allowed_tools: [Read, Glob, Grep, Skill]
---

While I was on ORB-12244 the macOS sandbox denied `openpty`, so the PTY-based integration tests hung until the timeout. Record that as friction.
