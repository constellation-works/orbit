---
description: Resolving a friction over MCP uses friction_update with status resolved; there is no MCP resolve tool.
tags: [friction]
max_turns: 15
allowed_tools: [Read, Glob, Grep, Skill, Bash]
---

F2026-09-012 is fixed now — the sandbox profile allows the registry path. Resolve it.
