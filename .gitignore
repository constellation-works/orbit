/target

# Agent / tool state directories (Claude Code, Codex, Gemini, Grok Build)
.claude/
.codex/
.gemini/
.grok/

agentspace
!.claude/settings.json
!.claude/hooks/
!.codex/
.codex/*
!.codex/config.toml
!.codex/hooks/
.codex/hooks/*
!.codex/hooks/orbit-learning-reminder
!.gemini/
.gemini/*
!.gemini/settings.json
!.gemini/hooks/
.gemini/hooks/*
!.gemini/hooks/orbit-learning-reminder
!.grok/
!.grok/config.toml
!.grok/hooks/
# The distributable agent plugin is repository content, including its hidden
# Claude/Codex manifests and generated-from-canonical skill mirror.
!plugin/.mcp.json
!plugin/.claude-plugin/
!plugin/.codex-plugin/
!plugin/skills/
# OS / editor
.DS_Store
.vscode/settings.json

# Scratch / session files
sessions
tmp
raw
examples.md
feature.md
feat.md
schema.md
critique.md

# Runtime state

orbit.db
orbit.db-shm
orbit.db-wal

# Orbit per-user state — not a repository artifact.
.orbit/
.monodev/

# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.venv/
uv.lock

# Node
node_modules/
dist/
build/
.astro/

# direnv — per-repo env vars (holds secrets like ANTHROPIC_API_KEY)
.envrc
.direnv/
.gstack/
.locks
*.lock
# npm lockfiles are the Dependabot / GitHub security-alert manifest for JS pins.
!package-lock.json
# Plugin fixtures commit the uv.lock their `uv run --frozen` backend installs from.
!crates/orbit-tools/tests/fixtures/plugins/*/uv.lock
.vscode
.grok
.gemini
.cowork-launch-env.txt
