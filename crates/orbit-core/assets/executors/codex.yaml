schemaVersion: 2
kind: Executor
metadata:
  name: codex
spec:
  executor_type: direct_agent
  command: codex
  args:
    - exec
    - --json
  stdout_format: envelope
  sandbox: macos-sandbox-exec
  model_flag: "--model"
  env: {}
