schemaVersion: 2
kind: Executor
metadata:
  name: gemini
spec:
  executor_type: direct_agent
  command: gemini
  args:
    - --approval-mode
    - yolo
    - --allowed-mcp-server-names
    - orbit
    - -o
    - json
  stdout_format: envelope
  sandbox: macos-sandbox-exec
  model_flag: "-m"
  env: {}
