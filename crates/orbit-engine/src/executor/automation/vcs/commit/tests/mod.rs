#![allow(missing_docs)]

mod already_landed;
mod author;
mod base_checkpoint;
mod candidate_paths;
mod delivery_gate;
mod git_ops;
mod message;
mod scope;
mod summary;
pub(in crate::executor::automation::vcs::commit) mod test_support;
