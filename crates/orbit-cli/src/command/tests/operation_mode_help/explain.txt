Explain the effective operation policy and authority

Usage: orbit operation explain [OPTIONS]

Options:
      --preset <PRESET>
          Run-layer preset override: supervised or autonomous (resets the preset-managed fields)
      --root <ROOT>
          Override the Orbit root directory (highest precedence)
      --completion <COMPLETION>
          Run-layer completion preference: review or done (an explicit done past the delivery cap is refused)
      --workspace <SELECTOR>
          Select a workspace by registered name, logical ID (`ws_*`), or absolute checkout path. Distinct from `--root`, which overrides the Orbit data directory. Only active workspaces may be bound; commands fail if the workspace status is not active
      --leaf-ceiling <LEAF_CEILING>
          Run-layer ceiling on concurrently live leaf runs
      --recovery-episodes <RECOVERY_EPISODES>
          Run-layer recovery episodes allowed per task
      --recovery-minutes <RECOVERY_MINUTES>
          Run-layer recovery wall-time minutes allowed per task
      --review-policy <REVIEW_POLICY>
          Run-layer review policy: none, before-pr (holds PR creation for a fresh reviewer), or after-landing
      --json
          Output as JSON
  -h, --help
          Print help
