orbit config — effective

Layers (later wins)          built-in defaults → global <global-root>/config.toml → workspace <workspace-root>/config.toml (absent)

Machine (machine.*)          who this machine is and how it bounds workers (global config only)
  id                               –                     unset    Stable generated identity of this machine (hm_...). Written once by `orbit init` and never reused; not settable.
  name                             –                     unset    Operator-chosen display name for this machine. The one `[machine]` value that may change: `orbit config set --global machine.name <value>`.
  task_prefix                      –                     unset    Immutable task-id namespace for ids minted on this machine (2-5 uppercase ASCII letters). Chosen once by `orbit init`; not settable.
  worker_containment               true                  default  Launch each detached pipeline worker in its own transient systemd user scope bounded by the machine.worker_* limits (Linux with a systemd user manager). false, or no reachable user manager, launches workers in the caller's cgroup with a warning.
  worker_memory_high               40%                   default  MemoryHigh= for each contained worker scope, where the kernel starts throttling the run: bytes with an optional K/M/G/T suffix, a percentage of physical RAM, or infinity (default 40%).
  worker_memory_max                50%                   default  MemoryMax= for each contained worker scope, where the kernel OOM-kills inside the run instead of the host: bytes with an optional K/M/G/T suffix, a percentage of physical RAM, or infinity (default 50%).
  worker_tasks_max                 4096                  default  TasksMax= for each contained worker scope: processes and threads the run may hold at once before fork/clone fails (>= 1, default 4096).

Delivery (workflow.*)        how tasks are shipped
  base_branch                      main                  global   Config fallback for ship/auto/pilot base branch when no registered workspace base_branch is bound.
  default_crew                     sol                   global   Named crew used when a task does not declare `crew` and no CLI override is given.
  system_crew                      system                default  Named crew used by system activities such as step-failure recovery and the task pilot.
  auto_ship                        false                 default  Opt-in for unattended ship dispatch via the routine/sweep scheduler.
  required_validation_commands     []                    default  Commands a distributed execution claim must pass on its exact candidate before this owner accepts its delivery handoff; empty means no claimed handoff can be accepted.
  low_complexity_crews             []                    default  Weighted crew pool for unassigned low-complexity tasks in drains and ships; entries are `name` or `name:weight` (all bare or all weighted); empty disables the pool.
  medium_complexity_crews          []                    default  Weighted crew pool for unassigned medium-complexity tasks in drains and ships; entries are `name` or `name:weight` (all bare or all weighted); empty disables the pool.
  hard_complexity_crews            []                    default  Weighted crew pool for unassigned hard-complexity tasks in drains and ships; entries are `name` or `name:weight` (all bare or all weighted); empty disables the pool.
  xhard_complexity_crews           []                    default  Weighted crew pool for unassigned xhard-complexity tasks in drains and ships; entries are `name` or `name:weight` (all bare or all weighted); empty disables the pool.

Crews (crews.*)              2 defined, from built-in and global
  NAME    FROM      PROVIDER  MODEL      EFFORT  TAGS  DESCRIPTION
  sol     global    codex     gpt-6-sol  medium  –  ← workflow.default_crew
  system  built-in  codex     gpt-6-sol  medium  –  ← workflow.system_crew

Execution (execution.*)      how agent subprocesses run
  codex.sandbox                    workspace-write       default  Codex sandbox mode: one of read-only, workspace-write, danger-full-access.
  codex.approval_policy            –                     unset    Codex approval policy: one of untrusted, on-request, never.
  env.pass                         HOME, PATH, CODEX_HOME, TMPDIR, USER  default  Environment variable names allow-listed for passthrough into agent subprocesses.
  env.inherit                      false                 derived  Not settable: an agent subprocess environment is always composed from execution.env.pass.

Review (operation.*)         automatic review policy — 5 keys unset (pass --all to list them)

Housekeeping                 logs, scoring, ids, plugins, and PR links
  scoring.enabled                  false                 global   Whether scoreboard metrics are recorded for task runs.
  automation.stall_window_minutes  60                    default  Minutes a deferred delivery-automation reason may persist before the evaluator logs it at warn and files one friction (1..=1440).
  runtime.log_retention_days       7                     default  Delete JSONL log archives whose mtime is older than this many days (must be >= 1).
  runtime.log_max_total_mb         500                   default  Total size budget (MiB) across JSONL log archives; oldest are pruned first when exceeded (must be >= 1).
  runtime.log_max_file_mb          100                   default  Roll the active JSONL log once it grows past this many MiB (must be >= 1 and <= runtime.log_max_total_mb).
  tasks.id_start                   –                     unset    Floor for the local task-id allocator on this machine (forward-only; lets machines hold disjoint id ranges).
  pr.task_url_template             –                     unset    URL template used to link a task ID in PR descriptions.
  plugin.legacy_callback_identity  false                 default  Deprecated: also accept the environment token and process ancestry as a plugin callback credential. Off by default; identity is the session record the host hands a backend on file descriptor 3. Removed in the next release.

Paths
  global root                            <global-root>
  workspace root                         <workspace-root>
  local root                             <workspace-root>
  audit db                               <global-root>/orbit.db
  semantic db                            <workspace-root>/state/semantic.db
  activities, executors, jobs, policies  <global-root>/resources/
  skills                                 <workspace-root>/resources/skills
